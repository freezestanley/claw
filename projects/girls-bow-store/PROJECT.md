# PROJECT.md — girls-bow-store

- **项目 slug**：`girls-bow-store`
- **项目名**：Ribbonè · 女生蝴蝶结饰品网店
- **类型**：单页面营销型网店（品牌展示 + 商品橱窗）
- **主题**：女生蝴蝶结 / 缎带发饰（hair bow / ribbon hair accessory）
- **设备重点**：PC 为主（用户明确要 PC 网店），同时覆盖 Pad / H5
- **技术栈**：Vite + 原生 JS + Tailwind(CDN) + anime.js(CDN)
- **语言**：JavaScript（禁止 TS）
- **状态**：active（开发中，未 build）

## 一句话定位

给学生女孩和甜系女生的蝴蝶结发饰小店——缎带、丝绒、珍珠蝴蝶结，
把温柔系在发间。

## 关键约定

- 一个 session = 一个项目，本 session 锁定 `girls-bow-store`（见 `.webgen/session-lock.json`）。
- 默认单页面、PC 优先、可本地 Vite 预览。
- 配图：优先真实图，视觉模型不可用 → 产品主视觉用程序化 SVG 蝴蝶结（绝对贴题、零破图），真实图用于氛围/lookbook 背景并在 ASSETS.md 标注「待人工核对」。

## 预览

- 端口：`4276`（`http://127.0.0.1:4276/`）
- 命令：`pnpm install` → `pnpm dev`

详见 HANDOFF.md。

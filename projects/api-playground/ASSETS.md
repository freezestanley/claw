# Assets

本项目为开发者联调工具，**无需图片素材**。

- Logo：无（用文字标题 + emoji/字符图标）。
- 图标：使用字符/emoji，避免额外图库依赖。
- 字体：系统 UI 字体 + 等宽（JetBrains Mono via CDN 或系统 monospace 回退）呈现响应体/token。
- 品牌色：中性 slate + emerald 强调（dev-tool 风格）。
- 占位策略：无图片，无需占位。

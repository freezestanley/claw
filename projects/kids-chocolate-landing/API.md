# API.md — 接口契约

## 结论：无远端接口依赖
本项目为纯静态单页营销落地页：
- CTA「了解更多 / 哪里购买」为**轻量交互**（锚点滚动 / 占位提示），**非真实结算、非真实下单**。
- 无用户数据采集、无登录、无支付。
- 无需本地 `/api` 代理（模板默认 `/api/health` 探活在本项目预期为 warning，不阻塞）。

## 若未来扩展
- 如需「门店查询 / 留资」可再引入 `/api` 代理与契约，目前不在范围内。

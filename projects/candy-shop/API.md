# API.md — candy-shop

## 当前状态
- **无真实远端接口**。购物车为纯前端轻量实现（内存状态 + 角标 + 抽屉），非真实结账。
- 邮箱订阅为前端占位（提交后本地提示成功，不发请求）。

## 本地代理
- vite.config.js 保留 `/api` → 占位 target（`http://127.0.0.1:8787`），当前页面未使用。
- 后续如接入真实商品/下单接口，开发期统一走 `/api` 代理。

## Mock 数据
- 商品列表、分类、评价均为页面内 mock 常量，可直接替换为接口返回结构。

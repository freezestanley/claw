# Session: 2026-06-12 17:15:36 GMT+8

- **Session Key**: agent:webgen:proj-hi-sky-world-smoke
- **Session ID**: 238ebc64-054f-4919-9231-df03d93a4f79
- **Source**: gateway:sessions.reset

## Conversation Summary

assistant: Server up on 4500, HTTP 200, content present. Now CDP verification — let me find or start a Chrome with remote debugging.
assistant: No existing CDP instance. Let me start a clean Chrome with remote debugging on an isolated user-data dir.
assistant: CDP is up. Let me navigate to the page, verify DOM, and capture a screenshot.
assistant: Need PUT for `/json/new`. Let me fix.
assistant: DOM verified (`hi sky world`, visible width > 0) and screenshot captured. Let me visually confirm it's on-theme.
assistant: Image tool can't access /tmp. Let me move it into the project dir and retry.
assistant: Image model unavailable (403). Per workspace rule, I skip the screenshot-level visual check and rely on the DOM + HTTP verification already done (text present, element visible, gradient applied via CSS). Now let me open it in CDP (visible, reusing the approach) for the user to view and report.

The page is verified. Let me clean up the headless CDP target and report. The preview server stays running on 4500.
assistant: Image model unavailable (403). Per workspace rule, I skip the screenshot-level visual check and rely on the DOM + HTTP verification already done (text present, element visible, gradient applied via CSS). Now let me open it in CDP (visible, reusing the approach) for the user to view and report.

The page is verified. Let me clean up the headless CDP target and report. The preview server stays running on 4500.
user: 加一条约束 新建项目 必须基于templates下的模版来生成（可通过复制模版文件来实现） 禁止自动编写
user: 加一条约束 新建项目 必须基于templates下的模版来生成（可通过复制模版文件来实现） 禁止自动编写
assistant: 明白。这是一条新约束。让我先看看 templates 目录的实际结构，再把它写入工作区规则。
assistant: 有两套模版：`demo-site` 和 `vite-page`，结构清晰（`scaffold/` 脚手架 + `post-init/*.tpl` 文档模板）。现在把这条约束写入 AGENTS.md，作为一条常驻硬约束（SO-007）。
assistant: I'll add SO-007 right after the SO-006 block. Let me find a clean insertion anchor.
assistant: 约束已写入。我同时把这条通用经验记入 `memory/`，避免后续 session 漏掉。
assistant: 已落地这条新约束。

**改动点**
- AGENTS.md - 新增 **SO-007**「新建项目必须基于 templates 模版生成，禁止手写脚手架」，并在「产出约束」追加对应一条。
- memory/conventions.md - 记入该长期约束，供后续 session 遵循。

**约束要点**
- 新项目一律从 `templates/demo-site` 或 `templates/vite-page` 复制 `scaffold/` + 渲染 `post-init/*.tpl` 生成；不确定时优先 `vite-page` 单页模版。
- 仅在模版复制完成后才改写页面业务代码；禁止从零手写 `index.html` / `package.json` / 目录结构。
- 模版缺能力时先反馈或请求新增模版，不绕过模版自建结构。

**问题**
- 无。

**说明**
- 当前 session 锁定项目为 `hi-sky-world`（已交付的冒烟页是在本约束生效前手写的），本条约束对**今后新建项目**生效。

下一步：如需我把已交付的 `hi-sky-world` 也按 `vite-page` 模版迁移一版，可以说一声；否则保持现状即可。

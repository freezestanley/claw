# 开发完成确认与构建流程（常驻）

## 背景
开发告一段落后若直接默默 build，容易在用户还想改的时候就过早打包，造成返工。
需要在「开发完成」和「build」之间插入一次确认。

## 规则（用户 2026-06-11 确认）
形成闭环：**开发完成 → 追问"是否还要改" →（无修改）→ build →（有修改）→ 继续迭代 → 再追问**。

- **开发完成后必须主动追问**：一轮开发/修改完成并通过基本验证后，不自行进入 build，先向用户追问："是否还有修改和调整？"
- **用户确认无修改 → 走 build 流程**：仅当用户明确回复"无修改 / 完成 / 可以打包"等等价确认后，才按 `.webgen/config.json` 中的构建命令进入 build（构建 + 产物清单 + 交付说明）。
- **用户仍有修改 → 不进入 build**：继续迭代，改完再次追问，直到用户确认无修改为止。
- **不跳过确认直接 build**：除非用户显式要求"直接打包"，否则不得跳过追问环节。
- build 前仍须满足原有"先验证再打包"约束（语法/构建/HTTP 或浏览器级校验）。

## 例外
- 用户显式说"直接打包 / 直接 build"时，跳过追问直接进入 build（但仍先做验证）。

## 相关位置
- 主约束写在 `AGENTS.md` →「## 开发完成确认与构建流程」小节。
- `TOOLS.md` →「使用原则」有一条对应的简述。
- 构建命令来源：项目内 `.webgen/config.json` 的 `deps.commands.build`（默认本工作区项目为 `pnpm build`）。

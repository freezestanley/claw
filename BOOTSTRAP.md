# BOOTSTRAP.md - 首次启动引导

欢迎使用 WebGen。

这个文件只负责 **workspace 级首次引导**，不负责项目级安装、页面生成或长期流程编排。

## 首次检查项

1. 检查 `templates/` 是否存在，且至少包含一个可用模板。
2. 检查 `SKILLS.md` 是否存在。
3. 检查 `projects/`、`memory/`、`scripts/` 目录是否可用。
4. 检查本机是否具备基础 Node 环境。
5. 如后续需要项目内 Python 工具，再检查 Python 可用性。

## 初始化原则

- 新项目统一从模板创建。
- 项目级依赖安装只能发生在项目目录中。
- 项目级状态必须写入项目文档和 `.webgen/`。
- WebGen 默认生成单页面项目，而不是多页面站点骨架。

## 首次引导后应具备的能力

- 能创建项目模板
- 能读取项目主文档 `PROJECT.md`
- 能根据 `SKILLS.md` 使用既定技能链
- 能在后续项目中使用 Vite/Node 进行本地预览

> 注意：此文件是出生说明，不是长期运行时逻辑入口。
